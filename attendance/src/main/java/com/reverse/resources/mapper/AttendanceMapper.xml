<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.reverse.attendance.internal.persistence.AttendanceMapper">

    <insert id="insertCheckIn" useGeneratedKeys="true" keyProperty="attendanceId">
        INSERT INTO attendance_record (
            employee_id, work_date, check_in_time, status, tardy_reason
        ) VALUES (
                     #{employeeId}, #{workDate}, #{checkInTime}, #{status}, #{tardyReason}
                 )
    </insert>

    <select id="findByEmployeeIdAndWorkDate" resultType="com.reverse.attendance.internal.domain.Attendance">
        SELECT
            attendance_id as attendanceId,
            employee_id as employeeId,
            work_date as workDate,
            check_in_time as checkInTime,
            check_out_time as checkOutTime,
            status as status,
            tardy_reason as tardyReason,
            modify_reason as modifyReason
        FROM attendance_record
        WHERE employee_id = #{employeeId} AND work_date = #{workDate}
    </select>

    <update id="updateCheckOut">
        UPDATE attendance_record
        SET check_out_time = #{checkOutTime},
            status = #{status}
        WHERE attendance_id = #{attendanceId}
    </update>

    <update id="updateAttendanceByAdmin">
        UPDATE attendance_record
        SET check_in_time = #{checkInTime},
            check_out_time = #{checkOutTime},
            status = #{status},
            modify_reason = #{modifyReason}
        WHERE attendance_id = #{attendanceId}
    </update>

</mapper>