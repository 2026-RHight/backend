<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.reverse.hr.internal.persistence.EmployeeMapper">

    <select id="findUserByEmployeeNum"
            parameterType="String"
            resultType="com.reverse.core.security.EmployeeAuthInfoDTO">
        SELECT employee_id   AS employeeId,
               employee_num  AS employeeNum,
               password      AS password
        FROM employee
        WHERE employee_num = #{employeeNum}
    </select>

    <select id="findRolesByEmployeeId"
            parameterType="Long"
            resultType="String">
        SELECT r.role_code
        FROM employee_role er
                 JOIN role r ON er.role_id = r.role_id
        WHERE er.employee_id = #{employeeId}
    </select>

</mapper>