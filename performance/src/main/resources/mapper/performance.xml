<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reverse.performance.internal.persistence.PerformanceMapper">
    <insert id="savePerformance"
            parameterType="map"
            useGeneratedKeys="true"
            keyProperty="request.performanceId"
            keyColumn="performance_id">
        INSERT INTO performance(
        employee_id, title, work_item,
        start_date, expected_end_date, work_detail, status,
        achievement_rate, difficulty_score, comment, feedback, created_at
        ) VALUES (
        #{employeeId}, #{request.title}, #{request.workItem, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
        #{request.startDate}, #{request.expectedEndDate}, #{request.workDetail}, #{request.status, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
        #{request.achievementRate}, #{request.difficultyScore}, #{request.comment}, #{request.feedback}, NOW()
        )
    </insert>
    <insert id="savePerformancePersonal"
            parameterType="com.reverse.performance.internal.dto.request.PerformancePersonalRequest">
        INSERT INTO performance_personal (performance_id, expected_value, result_summary,
        growth_point, improvement
        ) VALUES ( #{performanceId},
        #{expectedValue}, #{resultSummary},
        #{growthPoint}, #{improvement}
        )
    </insert>
    <insert id="savePerformanceTeam"
            parameterType="com.reverse.performance.internal.dto.request.PerformanceTeamRequest">
        INSERT INTO performance_team (performance_id, weight, team_result_summary, special_point
        ) VALUES (
        #{performanceId}, #{weight},
        #{teamResultSummary}, #{specialPoint}
        )
    </insert>
</mapper>
