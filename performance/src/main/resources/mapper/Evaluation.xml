<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reverse.performance.internal.persistence.EvaluationMapper">
    <insert id="saveEvaluation"
            parameterType="com.reverse.performance.internal.dto.request.EvalRequest">
        INSERT INTO evaluation(
            employee_id, evaluator_id, year, evaluation_score, confirmed_at, created_at)
        VALUES (
            #{employeeId}, #{evaluatorId}, #{year}, #{evaluationScore}, NOW(), NOW()
        )
    </insert>
</mapper>
