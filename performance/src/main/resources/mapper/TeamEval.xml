<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reverse.performance.internal.persistence.TeamEvalMapper">
    <select id="countByEvaluatorIdAndAppraiseeIdAndYear" resultType="int">
        SELECT COUNT(*)
        FROM team_evaluation
        WHERE evaluator_id = #{evaluatorId}
          AND appraisee_id = #{appraiseeId}
          AND performance_year = #{evaluationYear}
    </select>

    <insert id="saveTeamEval"
            parameterType="com.reverse.performance.internal.dto.request.TeamEvalRequest">
        INSERT INTO team_evaluation (
            evaluator_id,
            appraisee_id,
            performance_year,
            performance_score,
            performance_comment,
            attitude_score,
            attitude_comment,
            collaboration_score,
            collaboration_comment,
            creativity_score,
            creativity_comment,
            created_at
        )
        VALUES (
            #{evaluatorId, jdbcType=BIGINT},
            #{appraiseeId, jdbcType=BIGINT},
            #{evaluationYear, jdbcType=INTEGER},
            #{performanceScore, jdbcType=INTEGER},
            #{performanceComment, jdbcType=VARCHAR},
            #{attitudeScore, jdbcType=INTEGER},
            #{attitudeComment, jdbcType=VARCHAR},
            #{collaborationScore, jdbcType=INTEGER},
            #{collaborationComment, jdbcType=VARCHAR},
            #{creativityScore, jdbcType=INTEGER},
            #{creativityComment, jdbcType=VARCHAR},
            NOW()
        )
    </insert>
</mapper>
