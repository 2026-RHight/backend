<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reverse.core.mapper.SequenceMapper">

    <update id="updateSequenceNumber">
        UPDATE sequence_doc
        SET last_doc = last_doc + 1
        WHERE prefix = #{prefix, jdbcType=CHAR}
                  AND year = #{year, jdbcType=CHAR}
    </update>

    <select id="getSequenceNumber" resultType="int">
        SELECT last_doc
        FROM sequence_doc
        WHERE prefix = #{prefix, jdbcType=CHAR}
                  AND year = #{year, jdbcType=CHAR}
    </select>

    <insert id="insertInitialSequence">
        INSERT INTO sequence_doc (prefix, year, last_doc)
        VALUES (
                   #{prefix, jdbcType=CHAR},
                   #{year, jdbcType=CHAR},
                   1
               )
    </insert>

</mapper>